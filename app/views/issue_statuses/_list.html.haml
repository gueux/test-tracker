%table
  %thead
    %tr
      %th Name
      %th Closed?
      %th{:colspan => "3"}

  %tbody
    - statuses.each do |status|
      %tr
        %td= status.name
        %td= status.is_closed
        %td= link_to 'Destroy', issue_status_path(status.id), method: :delete, data: { confirm: 'Are you sure?' } 

